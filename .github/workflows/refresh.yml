name: Dashboard Refresh

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'

jobs:
  refresh:
    runs-on: self-hosted
    steps:
      - name: Run dashboard update
        shell: cmd
        run: '"C:\Users\bearley\OneDrive - Tifosi Optics\Whatagraph Replacement\run_dashboard_update.bat"'
